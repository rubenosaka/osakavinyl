import { CONSOLE } from '../constants/actionTypes';

const navBar = (navBar = [], action) =>{

    switch(action.type) {

        case CONSOLE:

            return navBar;
       
              
        default:

            return vinyl;

    }

}

export default navBar;